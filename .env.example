# Copy this file to .env and fill in your values
# .env is gitignored and should never be committed

# Databricks service principal token (used for agent endpoint auth fallback)
DATABRICKS_TOKEN=<YOUR_DATABRICKS_SERVICE_PRINCIPAL_TOKEN>

# Local dev PAT — used when running locally where user token is not available
LOCAL_DEV_PAT=<YOUR_DATABRICKS_PAT>

# Agent endpoint URL
AGENT_ENDPOINT_URL=https://5110247008182190.0.gcp.databricks.com/serving-endpoints/multi_agent_genie_20feb/invocations

# Agent endpoint name (for MLflow deployments fallback)
AGENT_ENDPOINT_NAME=multi_agent_genie_20feb

# Environment (dev or prod)
ENV=dev
